# Gateway IAM Configuration

# Application
DEBUG=false
SECRET_KEY=change-me-in-production

# Database
DATABASE_URL=postgresql+asyncpg://gateway:gateway@localhost:5434/gateway
REDIS_URL=redis://localhost:6379/0

# MidPoint
MIDPOINT_URL=http://localhost:8080/midpoint
MIDPOINT_USER=administrator
MIDPOINT_PASSWORD=admin

# LDAP/AD
LDAP_HOST=localhost
LDAP_PORT=10389
LDAP_BIND_DN=cn=admin,dc=example,dc=com
LDAP_BIND_PASSWORD=secret
LDAP_BASE_DN=dc=example,dc=com

# Odoo
ODOO_URL=http://localhost:8069
ODOO_DB=odoo
ODOO_USER=admin
ODOO_PASSWORD=admin

# SQL Target (Intranet)
INTRANET_DB_URL=postgresql://intranet:intranet@localhost:55432/intranet

# Keycloak
KEYCLOAK_URL=http://localhost:8081
KEYCLOAK_REALM=gateway
KEYCLOAK_CLIENT_ID=gateway-client
KEYCLOAK_CLIENT_SECRET=

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo-preview

# Vector Store
QDRANT_HOST=localhost
QDRANT_PORT=6333

# JWT
JWT_SECRET_KEY=jwt-secret-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# Workflow
WORKFLOW_DEFAULT_TIMEOUT_HOURS=72
WORKFLOW_MAX_LEVELS=5

# CORS
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

# Logging
LOG_LEVEL=INFO
